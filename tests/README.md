# tests

Automated checks for ingest, clean, and QC steps. Uses `testthat` (see `tests/testthat/`); add small fixtures in `tests/testthat/` or alongside the data they exercise. Add integration tests as you wire scripts into the new folders.
